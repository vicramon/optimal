%h1
  Today's Biggest Losers.
  .criteria
    Pulled from WSJ Composite List. Criteria: (20 &lt; Price &lt; 100, Lost More than 5%)

%br
%br
%table
  %tr
    %th.left Name
    %th Ticker
    %th Loss Today
    %th Current Price
    %th Earnings
    %th Next Earnings Call
    %th Chart
  - @stocks.each_with_index do |stock, index|
    %tr{ class: cycle('', 'gray') }
      %td.name= stock.name
      %td= stock.symbol
      %td= "#{stock.percent_change.to_f}%"
      %td= number_to_currency stock.last_trade_price_only
      %td
        = link_to "Quarterly", "http://finance.yahoo.com/q/is?s=#{stock.symbol}"
        &nbsp;|&nbsp;
        = link_to "Annual", "http://finance.yahoo.com/q/is?s=#{stock.symbol}&annual"
      %td= @call_dates[index].html_safe
      %td= link_to "Chart", "http://finance.yahoo.com/q/bc?s=#{stock.symbol}+Basic+Chart&t=1d"
